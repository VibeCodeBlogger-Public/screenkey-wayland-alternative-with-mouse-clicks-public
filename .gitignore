# Build directories
/build/
/builddir/
build*/

# .deb packaging artifacts (built by scripts/build-deb.sh / release CI)
/deb-stage/
*.deb

# Compiled objects / binaries
*.o
*.a
*.so
*.so.*

# Editor / tooling
*.swp
*~
.cache/
compile_commands.json
.vscode/
